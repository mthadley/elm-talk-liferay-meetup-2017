const myGreeting = "Hello Everyone!";
window.message = myGreeting;

const items = [1, 2, 3];
assert.equal(length(items), 3); // Pass. Cool.

assert.equal(window.message, myGreeting) // Fail. Wait, why did my message change?
